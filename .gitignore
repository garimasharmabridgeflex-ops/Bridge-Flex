# Private keys — never commit these (APNs / App Store Connect API keys, etc.)
*.p8
*.p12
*.pem

# Training source videos — these live in Firebase Storage, not the repo.
*.mp4

# LibreOffice lock files
.~lock.*#
